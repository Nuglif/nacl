# ok
.include "sample.conf";


parameters {
 	password .vault (default=lol) "foo";
}

param value;
section {
	#lol
    toto = .consul "${PREFIX}/foo/bar";
    param1 "%password%";
    flag true;
    number 10k;
	size = 1mb;
	time: 0.2s;
    string = "something";
    subsection {
        host = {
            host = "hostname";
            port = 900;
        }
        host = {
            host = "hostname \nc'est\tune chaine \"longue";
            port = 901;
			.include "sample.conf";
        }
    }
	pierrick <<<EOF
foo
ok
lol
EOF;
	foo = "bar ${BAR} ";
}
#foo

